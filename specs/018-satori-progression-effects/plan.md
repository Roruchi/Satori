# Implementation Plan: Satori Progression & Architectural Effects

**Branch**: `018-satori-progression-effects` | **Date**: 2026-03-28 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/018-satori-progression-effects/spec.md`

## Summary

Implement a deterministic Satori progression layer that adds a hard Satori cap, per-minute housed/unhoused spirit delta processing, four era thresholds with spirit-tier transitions, a runtime UI display for Satori amount/cap/era, and structure-tier cap expansion/effects including buildable dwellings plus unique monument enforcement at pattern-confirmation time. The approach reuses current Godot data-driven pattern/discovery pipelines by extending structure definitions and progression services instead of introducing parallel systems.

## Technical Context

**Language/Version**: GDScript on Godot 4.6  
**Primary Dependencies**: Godot runtime services, existing discovery/pattern stack, GUT (`addons/gut`)  
**Storage**: Existing game save/autoload persistence in `user://` and in-memory singleton state (no new storage backend)  
**Testing**: GUT unit tests + manual in-editor validation for progression/UI feedback and monument rejection flow  
**Target Platform**: Mobile-first gameplay targets (Android/iOS) with desktop-compatible editor/runtime  
**Project Type**: Single-player Godot game feature extension  
**Performance Goals**: Preserve current runtime responsiveness and 60 fps behavior while adding 60-second progression ticks  
**Constraints**: Satori must clamp to `[0, cap]`; era updates must be deterministic; unique monuments must be blocked pre-confirmation; no autoload/class_name naming collisions  
**Scale/Scope**: 1 global progression loop, 4 eras, 5 Tier 1 dwellings, 5 Tier 2 structures, 3 Tier 3 monuments, associated tests/docs/contracts

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Spec Traceability**: PASS. Plan maps directly to US1-US4 and FR-001..FR-024 in `spec.md`.
- **Godot-Native Fit**: PASS. Changes stay in Godot-native scripts/resources under `src/`, scene wiring in `scenes/`, and tests under `tests/unit`.
- **Validation Strategy**: PASS. Deterministic progression logic gets GUT coverage; UI/interaction rejection flow gets explicit manual checks.
- **World Rule Safety**: PASS. No permanence reset/undo changes; progression and gating remain deterministic and save-compatible by design.
- **Mobile Budgets**: PASS. Minute-cadence calculations and metadata checks are low-frequency and bounded.
- **Guardrails**: PASS. Plan explicitly preserves autoload naming guardrails, typed preload usage, and avoids risky inferred `Variant` typing in warnings-sensitive contexts.

## Project Structure

### Documentation (this feature)

```text
specs/018-satori-progression-effects/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── progression-signals-and-thresholds.md
│   └── unique-monument-confirmation.md
└── tasks.md  # generated by /speckit.tasks
```

### Source Code (repository root)

```text
project.godot

src/
├── autoloads/
│   ├── satori_service.gd
│   └── (progression state owner, if split)
├── satori/
│   ├── SatoriIds.gd
│   ├── SatoriConditionEvaluator.gd
│   └── conditions/
├── biomes/
│   ├── pattern_definition.gd
│   ├── pattern_matcher.gd
│   ├── discovery_catalog_data.gd
│   └── patterns/
│       ├── tier1/
│       ├── tier2/
│       └── tier3/
├── grid/
│   ├── PlacementController.gd
│   └── GardenView.gd
├── spirits/
│   └── spirit_service.gd
└── ui/
    └── (progression display/rejection feedback consumers)

tests/
└── unit/
    ├── test_satori_service.gd
    ├── patterns/
    │   └── test_pattern_loader.gd
    └── spirits/
        └── test_shrine_interact_flow.gd
```

**Structure Decision**: Extend existing progression/discovery/pattern flow in-place. Keep core calculations in autoload/service-level scripts and enforce uniqueness in existing confirmation/matcher paths rather than adding a new structure subsystem.

## Phase 0: Research Plan

Research topics generated from technical context and integration points:

1. Deterministic order-of-operations for mixed positive/negative/minute-based Satori deltas with cap/floor clamping.
2. Best-fit location for era evaluation and signaling to avoid duplicate transitions while preserving existing Satori condition unlock sequences.
3. Existing pattern confirmation path and earliest safe point to block unique monument confirmation with player-visible feedback.
4. Data-model extension strategy for structure uniqueness/effects using current `PatternDefinition` and catalog resources.

Phase 0 output is captured in `research.md` with decisions, rationale, and alternatives.

## Phase 1: Design and Contracts Plan

Design artifacts to produce:

1. `data-model.md` describing Satori state, era state, structure definitions/instances, and per-minute tick inputs.
2. `contracts/progression-signals-and-thresholds.md` documenting threshold math, emitted events, and gate-open semantics.
3. `contracts/unique-monument-confirmation.md` documenting pre-confirmation rejection behavior for unique monuments.
4. `quickstart.md` covering targeted GUT runs and manual validation scenarios (including blocked second monument attempt).

Agent context update step:

1. Run `/home/runner/work/Satori/Satori/.specify/scripts/powershell/update-agent-context.ps1 -AgentType claude`.
2. Confirm update completes and manual sections remain intact.

## Phase 2: Implementation Planning Outline

Ordered workstreams for `/speckit.tasks` generation:

1. **Progression Tick and Cap Core**
   - Add Satori cap/current state fields and deterministic minute processing.
   - Compute `delta = housed_count - (unhoused_count * 2)` with clamp to `[0, cap]`.
2. **Era State and Gate Signaling**
   - Implement Stillness/Awakening/Flow/Satori thresholds.
   - Emit `era_changed(new_era)` only on crossing boundaries.
   - Apply spirit-tier policy: Tier 2 above required era, Tier 3 (all Kami/deities) at higher era, Tier 4 (Sky Whale) at Satori era, and despawn when era drops below threshold.
   - Replace gate-style language in implementation with era-driven spirit summon/despawn checks.
3. **Structure Data and Cap Contributions**
   - Represent tier-specific cap contributions (+50/+250/+1000).
   - Author/verify structure catalog entries for all RFC-listed dwellings, pavilions, and monuments.
   - Ensure dwellings are explicitly buildable so spirit housing can function in gameplay.
4. **Unique Monument Enforcement in Confirmation Flow**
   - Add `is_unique` metadata to structure definitions.
   - Query active structure state before Bell confirmation; reject and highlight blocked blueprint.
5. **Structure Effect Wiring**
   - Implement Tier 2 local effects and Tier 3 monument effects (Great Torii burst, Pagoda passive/housing, Void Mirror island multiplier).
6. **Validation and Documentation Sync**
   - Add/extend GUT coverage for progression math, threshold crossing, uniqueness guards, and catalog effects.
   - Record manual checks and any UI evidence path in quickstart.
   - Validate runtime UI for Satori amount/cap/era visibility and update timing.
   - Validate spirit tier mapping updates (Mist Stag Tier 2, all Kami/deities Tier 3, Sky Whale Tier 4).

## Post-Phase 1 Constitution Re-check

- **Spec Traceability**: PASS. Data model and contracts map to FR/US items without drift.
- **Godot-Native Fit**: PASS. Design remains within existing Godot scripts/resources and autoload patterns.
- **Validation Strategy**: PASS. Automated deterministic checks plus manual confirmation-flow checks are explicit.
- **World Rule Safety**: PASS. Deterministic progression with no undo/reset semantics added.
- **Mobile Budgets**: PASS. Low-frequency minute updates and scoped checks keep runtime impact minimal.
- **Guardrails**: PASS. No planned autoload/class_name collision; typed dependency and explicit typing guidance retained.

## Complexity Tracking

No constitution violations currently require justification.
