# Pattern Definitions

This directory stores data-driven pattern resources used by the discovery engine.

Guidelines:
- Keep one discovery pattern per `.tres` file.
- Use stable `discovery_id` values that never change once shipped.
- Start from `deep_stand_cluster.tres` as a baseline fixture.
